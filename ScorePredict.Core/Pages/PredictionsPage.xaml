<?xml version="1.0" encoding="utf-8" ?>
<pages:ContentPageBase x:TypeArguments="viewModels:PredictionsPageViewModel" xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:pages="clr-namespace:ScorePredict.Core.Pages;assembly=ScorePredict.Core"
			 xmlns:viewModels="clr-namespace:ScorePredict.Core.ViewModels;assembly=ScorePredict.Core"
			 x:Class="ScorePredict.Core.Pages.PredictionsPage"
			 BackgroundColor="#77B739">

  <ListView ItemsSource="{Binding Path=Predictions, Mode=OneWay}">
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell>
          <StackLayout HorizontalOptions="Start" VerticalOptions="StartAndExpand"
                       Padding="10,5,5,5">
            <Grid HorizontalOptions="Start" VerticalOptions="StartAndExpand">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <Label Text="{Binding Path=AwayTeam, Mode=OneWay}" FontSize="40" />
              <Label Text="{Binding Path=HomeTeam, Mode=OneWay}" FontSize="40" Grid.Row="1" />
            </Grid>
          </StackLayout>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>

  <ContentPage.ToolbarItems>
    <ToolbarItem Name="Refresh">
      <ToolbarItem.Icon>
        <OnPlatform x:TypeArguments="FileImageSource"
                    WinPhone="Assets/appbar.refresh.png"
                    Android="ic_action_refresh.png" />
      </ToolbarItem.Icon>
    </ToolbarItem>
  </ContentPage.ToolbarItems>
</pages:ContentPageBase>